{
  "name": "@quant/report-studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build ./src/main.ts --outdir ./dist --target browser",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test src",
    "dev": "bun build ./src/main.ts --outdir ./dist --target browser --watch"
  },
  "dependencies": {
    "@quant/contracts": "workspace:*",
    "@quant/plotting": "workspace:*",
    "@quant/report-ui": "workspace:*"
  }
}
